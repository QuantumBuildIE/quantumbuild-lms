// Language options are now loaded dynamically from the Language lookup category.
// Use useLookupValues('Language') from @/hooks/use-lookups to get the list.
// This file is kept for backward compatibility — the export maps lookup values
// to the { value, label } format used by existing consumers.

// No more hardcoded language list — use the hook directly.
