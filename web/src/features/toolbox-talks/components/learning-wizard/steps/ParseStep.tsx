'use client';

// TODO (Phase 5.4): Implement Step 2 — Parse (AI content extraction).

export function ParseStep() {
  return (
    <div className="rounded-lg border border-dashed p-10 text-center text-muted-foreground">
      <p className="text-sm font-medium">Step 2 — Parse</p>
      <p className="text-xs mt-1">Placeholder — Phase 5.4</p>
    </div>
  );
}
