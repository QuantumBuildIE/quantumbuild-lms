'use client';

// TODO (Phase 5.5): Implement Step 6 — Validate (translation validation run + reviewer decisions).

export function ValidateStep() {
  return (
    <div className="rounded-lg border border-dashed p-10 text-center text-muted-foreground">
      <p className="text-sm font-medium">Step 6 — Validate</p>
      <p className="text-xs mt-1">Placeholder — Phase 5.5</p>
    </div>
  );
}
