# §5.18 Pre-flight: Frontend Test Framework

Date: 2026-06-15

## Summary

Clean slate. No existing test infrastructure. Safe to proceed with installation.

## package.json findings

- **React**: 19.2.1
- **Next.js**: 16.0.10
- **TypeScript**: ^5
- **Test-related dependencies**: None — no jest, vitest, @testing-library/*, jsdom present in either `dependencies` or `devDependencies`
- **Package manager**: npm (only `package-lock.json` found; no `pnpm-lock.yaml` or `yarn.lock`)

## tsconfig.json findings

- Single path alias: `@/*` → `./src/*`
- `moduleResolution`: `bundler`
- `jsx`: `react-jsx`
- No additional aliases beyond `@/*`

## next.config.ts findings

- Minimal config, no custom webpack configuration
- No environment setup or transpilation options that would affect test setup

## Existing test infrastructure

- **vitest.config.*** — none found
- **jest.config.*** — none found
- **\*.test.ts(x) / \*.spec.ts(x) in web/src** — none found

## Conclusion

Safe to install: vitest, @vitejs/plugin-react, @testing-library/react, @testing-library/jest-dom, @testing-library/user-event, jsdom.
