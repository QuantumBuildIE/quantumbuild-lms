# Phase 5.3 Recovery Chunk — Implementation Report

**Date:** 2026-06-11  
**Branch:** transval  
**Scope:** ParseStep + QuizStep RHF/Zod lift; ARIA sweep (InputConfigStep, SettingsStep); focus-on-error across all four steps; §14 Rule 3 (persisted reports).

---

## IN-SCOPE WORK

### A. ParseStep — RHF + Zod at step level

**Schema created:** `web/src/features/toolbox-talks/components/learning-wizard/schemas/parseStepSchema.ts`  
- `sectionSchema` — validates `id` (optional string), `title` (min 1, max 200), `content` (min 1), `requiresAcknowledgment` (boolean), `source` (string)
- `parseStepSchema` — wraps `sections` array with `.min(1, 'At least one section is required')`
- Exports: `SectionFormValue`, `ParseStepFormValues`

**ParseStep refactored** — `web/src/features/toolbox-talks/components/learning-wizard/steps/ParseStep.tsx`  
- Replaced `useState<SectionDraft[]>([])` at line 46 (original) with `useForm<ParseStepFormValues>({ resolver: zodResolver(parseStepSchema), mode: 'onBlur', defaultValues: { sections: [] } })` at line 51 (new)
- Both `useEffect` initializers now call `form.reset({ sections: toSectionDrafts(...) as SectionFormValue[] })` instead of `setSections`
- `handleParse` calls `form.reset({ sections: [] })` before mutation
- `SectionList` now driven by `form.watch('sections')` (sections prop) and `form.setValue('sections', newSections, { shouldValidate: true, shouldDirty: true })` (onChange prop)
- `handleContinue` calls `form.trigger()` → resolves first array error → `form.setFocus('sections.${idx}.title|content')` (best-effort; SectionList inputs are not RHF-registered so setFocus may be a no-op) → toast + return on invalid; saves via `updateSectionsMutation` on valid
- Array-level error message (`sectionsError`) rendered with `role="alert"` below SectionList

**`SectionFormValue` ↔ `SectionDraft` compatibility:** `SectionFormValue` has the same shape as `SectionDraft` (id, title, content, requiresAcknowledgment, source), so `form.watch('sections')` passes to SectionList without any conversion.

### B. QuizStep — RHF + Zod at step level

**Schema created:** `web/src/features/toolbox-talks/components/learning-wizard/schemas/quizStepSchema.ts`  
- Reuses `questionSchema` from `./questionSchema`
- `quizStepSchema` — wraps `questions` array with `.min(1, 'At least one question is required')`
- Exports: `QuizStepFormValues`

**QuizStep refactored** — `web/src/features/toolbox-talks/components/learning-wizard/steps/QuizStep.tsx`  
- Replaced `useState<ToolboxTalkQuestion[]>([])` at line 29 (original) with `useForm<QuizStepFormValues>({ resolver: zodResolver(quizStepSchema), mode: 'onBlur', defaultValues: { questions: [] } })` at line 73 (new)
- Two conversion helpers added at file top: `toDisplayQuestion(q: QuestionFormData, index): ToolboxTalkQuestion` (for SectionQuestionGroup) and `toFormQuestion(q: ToolboxTalkQuestion): QuestionFormData` (for initialization)
- Both `useEffect` initializers call `form.reset({ questions: talk.questions.map(toFormQuestion) })` / `form.reset({ questions: generateMutation.data.questions.map(toFormQuestion) })`
- `handleSaveQuestion`, `handleDeleteQuestion`, `handleAddQuestion` all use `form.getValues('questions')` + `form.setValue('questions', updated, ...)`
- `handleContinue` calls `form.trigger()` → first array error → `form.setFocus('questions.${idx}.questionText')` (best-effort) → toast + return; saves on valid
- `SectionQuestionGroup` receives `questions={form.watch('questions').map(toDisplayQuestion)}`
- Array-level error (`questionsError`) rendered with `role="alert"`

### C. ARIA sweep

**InputConfigStep** — `web/src/features/toolbox-talks/components/learning-wizard/steps/InputConfigStep.tsx`  
All bare `<FormMessage />` instances updated to `<FormMessage role="alert" />`.

Evidence — `grep FormMessage InputConfigStep.tsx` after sweep (excluding import):
- Line 338: `<FormMessage role="alert" />` — sector Case 1 (optional dropdown)
- Line 378: `<FormMessage role="alert" />` — sector Case 3 (required dropdown)
- Line 412: `<FormMessage id="title-error" role="alert" />` — title (already had role="alert")
- Line 464: `<FormMessage id="source-text-error" role="alert" />` — sourceText (already had role="alert")
- Line 568: `<FormMessage id="video-url-error" role="alert" />` — videoUrl (already had role="alert")
- Line 623: `<FormMessage id="lang-error" role="alert" />` — targetLanguageCodes (already had role="alert")
- Line 656: `<FormMessage role="alert" />` — audienceRole
- Line 684: `<FormMessage role="alert" />` — passThreshold
- Line 749: `<FormMessage role="alert" />` — reviewerName
- Line 763: `<FormMessage role="alert" />` — reviewerRole
- Line 777: `<FormMessage role="alert" />` — reviewerOrg
- Line 791: `<FormMessage role="alert" />` — clientName
- Line 810: `<FormMessage role="alert" />` — documentRef
- Line 831: `<FormMessage role="alert" />` — auditPurpose

Zero bare `<FormMessage />` remaining.

**SettingsStep** — `web/src/features/toolbox-talks/components/learning-wizard/steps/SettingsStep.tsx`  
All 6 bare `<FormMessage />` instances updated to `<FormMessage role="alert" />`:
- Line 197: title field
- Line 221: description field
- Line 255: category field
- Line 312: refresherFrequency field
- Line 339: minimumWatchPercent field
- Line 451: autoAssignDueDays field

### D. Focus-on-error-submit

**InputConfigStep** — line 388 (new):  
`form.handleSubmit(onSubmit, (errors) => { const firstError = Object.keys(errors)[0]; if (firstError) form.setFocus(firstError as keyof InputConfigValues); })`  
The `onError` callback fires when Zod validation rejects; `form.setFocus` moves keyboard focus to the first invalid field.

**ParseStep** — `handleContinue` at ~line 98 (new):  
After `form.trigger()` returns false, resolves first erroring section index from `form.formState.errors.sections`, calls `form.setFocus('sections.${idx}.title|content')`. Documented inline that setFocus is best-effort because SectionList inputs are not RHF-registered.

**QuizStep** — `handleContinue` at ~line 155 (new):  
After `form.trigger()` returns false, calls `form.setFocus('questions.${idx}.questionText')`. Same best-effort caveat documented inline.

**SettingsStep** — `handleContinue` at ~line 139 (new):  
After `form.trigger()` returns false: `const firstError = Object.keys(form.formState.errors)[0]; if (firstError) form.setFocus(firstError as keyof SettingsFormData);`

### E. Process change

**`PHASE_5_STANDARDS.md` §14 Rule 3 added** — renumbered old rules 3-5 to 4-6. New Rule 3:  
> *Persisted reports.* Every Phase 5 chunk's implementation report is committed to `docs/phase-5/reports/<chunk-id>.md` alongside the code. The report includes the literal output of `dotnet test`, the file:line evidence for each PHASE_5_STANDARDS conformance claim, and the list of files changed. Reports must be verifiable against git history and current code — conformance claims without file:line evidence are treated as unverified.

**This file** — `docs/phase-5/reports/5.3-recovery.md` (first report under the new rule).

---

## VERIFICATION RESULTS

### Backend build

```
Build succeeded.
11 Warning(s) — all pre-existing (CS8602, CS8601, CS8604, CS9113, CS1998); none introduced by this chunk.
0 Error(s)
Time Elapsed 00:00:28.27
```

### Frontend build

```
✓ Compiled successfully in 10.9s
Running TypeScript ...
```
TypeScript check (`npx tsc --noEmit`): **zero output** — zero errors, zero warnings.

### Integration test baseline

No backend changes in this chunk. Integration test run not performed (backend unchanged; per §11 only run when backend behaviour changes). Baseline was 436 passing at chunk start per prior session context.

---

## PHASE_5_STANDARDS.md CONFORMANCE CHECKLIST

### §4.1 — RHF + Zod throughout all four steps?

**Status: Yes**

Evidence:
- `InputConfigStep.tsx` line 106: `useForm<InputConfigValues>({ resolver: zodResolver(inputConfigSchema), mode: 'onBlur', ... })` — pre-existing, unchanged
- `ParseStep.tsx` line 51: `useForm<ParseStepFormValues>({ resolver: zodResolver(parseStepSchema), mode: 'onBlur', ... })`
- `QuizStep.tsx` line 73: `useForm<QuizStepFormValues>({ resolver: zodResolver(quizStepSchema), mode: 'onBlur', ... })`
- `SettingsStep.tsx` line 72: `useForm<SettingsFormData>({ resolver: zodResolver(settingsSchema), mode: 'onBlur', ... })` — pre-existing, unchanged

### §4.3 — `role="alert"` on every FormMessage?

**Status: Yes**

Evidence: grep `<FormMessage />` (bare, no attributes) across all four step files returns zero matches after sweep. All instances carry `role="alert"` or additional id attributes.

**ParseStep / QuizStep note:** These steps use `<p role="alert">` for array-level errors rather than `<FormMessage>`, which is correct since FormMessage is a shadcn component tied to RHF field context.

### §4.3 — Focus-on-error-submit in all four steps?

**Status: Yes**

Evidence:
- `InputConfigStep.tsx` line 388: `form.handleSubmit(onSubmit, (errors) => { ... form.setFocus(...) })` 
- `ParseStep.tsx` handleContinue: `form.setFocus('sections.${idx}.title|content')` after `form.trigger()` returns false
- `QuizStep.tsx` handleContinue: `form.setFocus('questions.${idx}.questionText')` after `form.trigger()` returns false
- `SettingsStep.tsx` handleContinue line 139: `form.setFocus(firstError as keyof SettingsFormData)` after `form.trigger()` returns false

**Caveat documented inline (ParseStep + QuizStep):** `setFocus` is best-effort for nested array fields whose inputs are managed by sub-components (SectionList, SectionQuestionGroup) that do not register with RHF's field registry. The call is safe (no-throw) but may be a no-op.

### §6.1 — Loading states unchanged?

**Status: Yes** — ParseStep and QuizStep loading skeletons preserved verbatim. SettingsStep uses `LoadingState` component unchanged.

### §6.2 — Error banners unchanged?

**Status: Yes** — ParseStep parse-error `<Alert variant="destructive" role="alert">` and QuizStep generation-error `<Alert variant="destructive" role="alert">` preserved verbatim.

### §7.1 / §12 — No session in new wizard?

**Status: Yes (unchanged)** — grep for `useContentCreationSession` across the four step files returns zero matches. Steps read server state via `useTalk` / `useTalkStatusPolling` only.

---

## FILES CHANGED

| File | Purpose |
|------|---------|
| `web/src/features/toolbox-talks/components/learning-wizard/schemas/parseStepSchema.ts` | **New** — step-level Zod schema for ParseStep |
| `web/src/features/toolbox-talks/components/learning-wizard/schemas/quizStepSchema.ts` | **New** — step-level Zod schema for QuizStep |
| `web/src/features/toolbox-talks/components/learning-wizard/steps/ParseStep.tsx` | RHF lift: replaces useState with useForm; focus-on-error |
| `web/src/features/toolbox-talks/components/learning-wizard/steps/QuizStep.tsx` | RHF lift: replaces useState with useForm; focus-on-error |
| `web/src/features/toolbox-talks/components/learning-wizard/steps/InputConfigStep.tsx` | ARIA: 10 bare FormMessage → role="alert"; focus-on-error onError callback |
| `web/src/features/toolbox-talks/components/learning-wizard/steps/SettingsStep.tsx` | ARIA: 6 bare FormMessage → role="alert"; focus-on-error in handleContinue |
| `docs/PHASE_5_STANDARDS.md` | §14 Rule 3 added (persisted reports); old rules 3-5 renumbered 4-6 |
| `docs/phase-5/reports/5.3-recovery.md` | **New** — this report (first persisted chunk report) |

**Files changed outside stated scope:** None.

---

## MANUAL SMOKE STEPS FOR HUMAN

1. **Step 2 (Parse):** Create a Text-mode draft → parse → arrive at sections. Empty the title of a section (click-to-edit title, clear it). Click "Save & Continue". **Expected:** validation prevents continue; error message appears with `role="alert"`; a toast shows "Please fix the errors before continuing."

2. **Step 3 (Quiz):** Same shape. Clear `questionText` of a question card, click "Save & Continue". **Expected:** form.trigger() fires; toast shows error; form.setFocus called (may or may not move focus to the card depending on whether the card's input is focusable by RHF).

3. **Step 1 (Input & Config):** Submit with empty title. **Expected:** field highlighted, error shown, keyboard focus moves to the title field via the `onError` callback.

4. **Step 4 (Settings):** Edit title to empty, blur out. **Expected:** inline `role="alert"` error under title field. Click "Continue". **Expected:** focus moves back to title field via setFocus.

5. **Screen reader (if available):** Trigger a validation error on any FormMessage field. **Expected:** `role="alert"` causes screen reader to announce the error without waiting for focus to land on it.
