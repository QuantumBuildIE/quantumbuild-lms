# Phase 5.5a — Backend Publish Endpoint: Implementation Report

**Date:** 2026-06-14  
**Branch:** transval  
**Status:** Complete

---

## 1. Test Results

**Before:** 637 passing (192 unit + 445 integration), 0 failing  
**After:** 645 passing (192 unit + 453 integration), 0 failing  
**Delta:** +8 new integration tests, 0 regressions

---

## 2. What Was Built

### New endpoint

`POST /api/toolbox-talks/{talkId}/publish`

- Auth: `Learnings.Manage` (action-level override over class-level `Learnings.View`)
- Fires `PublishToolboxTalkCommand(TalkId, TenantId)`
- Returns `PublishTalkResult { talkId, status, publishedAt }` directly (no `Result<T>` wrapper — matches new endpoint convention)
- Enqueues `RequirementMappingJob` fire-and-forget on success (via concrete class per CLAUDE.md Note 21)
- Returns 404 if talk not found (tenant isolation: cross-tenant talks appear as 404)
- Returns 409 (`WorkflowInvalidState`) for all business rule violations

### Preconditions checked (in order)

1. Talk exists for `{talkId}` and `TenantId` — otherwise 404
2. `talk.Status != Published` — otherwise 409 "already published"
3. `talk.Sections.Count > 0` — otherwise 409 "must have at least one section"
4. If `TargetLanguageCodes` is non-empty: at least one `TranslationValidationRun` with `Status == Completed` must exist — otherwise 409

### Mutation

- `talk.Status = Published`
- `talk.PublishedAt = DateTime.UtcNow`
- `talk.UpdatedAt = DateTime.UtcNow`

---

## 3. Files Changed (in scope)

### New files

| File | Purpose |
|---|---|
| `src/Modules/ToolboxTalks/QuantumBuild.Modules.ToolboxTalks.Application/Commands/PublishToolboxTalk/PublishToolboxTalkCommand.cs` | Command record + `PublishTalkResult` |
| `src/Modules/ToolboxTalks/QuantumBuild.Modules.ToolboxTalks.Application/Commands/PublishToolboxTalk/PublishToolboxTalkCommandHandler.cs` | Handler: gate checks + mutation |
| `src/Core/QuantumBuild.Core.Infrastructure/Migrations/20260614095742_AddPublishedAtToToolboxTalk.cs` | EF migration (CLI-generated) |
| `src/Core/QuantumBuild.Core.Infrastructure/Migrations/20260614095742_AddPublishedAtToToolboxTalk.Designer.cs` | EF migration Designer companion |
| `tests/QuantumBuild.Tests.Integration/ToolboxTalks/PublishToolboxTalkTests.cs` | 8 integration tests |
| `docs/phase-5/reports/5.5a-publish-backend-recon.md` | Recon report |
| `docs/phase-5/reports/5.5a-publish-backend.md` | This file |

### Modified files

| File | Change |
|---|---|
| `src/Modules/ToolboxTalks/QuantumBuild.Modules.ToolboxTalks.Domain/Entities/ToolboxTalk.cs` | Added `PublishedAt DateTime? { get; set; }` |
| `src/Modules/ToolboxTalks/QuantumBuild.Modules.ToolboxTalks.Application/DTOs/ToolboxTalkDto.cs` | Added `CreatedAt`, `UpdatedAt`, `PublishedAt` to Audit section |
| `src/Modules/ToolboxTalks/QuantumBuild.Modules.ToolboxTalks.Application/Queries/GetToolboxTalkById/GetToolboxTalkByIdQueryHandler.cs` | Mapped `CreatedAt`, `UpdatedAt`, `PublishedAt` in `MapToDto()` |
| `src/QuantumBuild.API/Controllers/ToolboxTalksController.cs` | Added `PublishByTalkId` action + `using` import |

---

## 4. Files Changed Outside Stated Scope

None.

---

## 5. Build Output

```
Passed! - Failed: 0, Passed: 192, Skipped: 0, Total: 192 — QuantumBuild.Tests.Unit.dll
Passed! - Failed: 0, Passed: 453, Skipped: 0, Total: 453 — QuantumBuild.Tests.Integration.dll
```

No new warnings introduced by this chunk.

---

## 6. Spot-Check Results

| Check | Result |
|---|---|
| Endpoint registered: `[HttpPost("{talkId:guid}/publish")]` at line 654 | ✓ |
| `Learnings.Manage` policy on action | ✓ |
| `RequirementMappingJob` enqueued via concrete class | ✓ |
| No `Result<T>` wrapper on success response (direct DTO) | ✓ |
| Migration files: both `.cs` and `.Designer.cs` present | ✓ |
| 8 integration tests in `PublishToolboxTalkTests.cs` | ✓ |
| Baseline held (0 regressions) | ✓ |

---

## 7. Key Design Decisions

**Why check any target language's completed run (not per-language)?**  
The gate is "at least one translation quality signal exists." A partially validated multilingual talk can still publish — the spec says `AwaitingThirdParty` doesn't block. Frontend (5.5b) handles per-language warnings.

**Why enqueue `RequirementMappingJob` from the controller?**  
Keeps Application layer clean from Hangfire dependency. Pattern matches existing `ContentCreationSessionService` (Infrastructure) doing fire-and-forget enqueues. Controller already has `using Hangfire;`.

**Why allow publish from any non-Published status?**  
The new wizard creates talks in `Draft`, but the system could theoretically set a talk to `ReadyForReview` via other paths. Restricting to `Draft` only would be unnecessarily brittle. Only `Published → Published` is blocked.

---

## 8. BACKLOG Entries Added

- **Pre-existing:** `CoverImageUrl` on `ToolboxTalk` entity and `ToolboxTalkDto` but missing from `GetToolboxTalkByIdQueryHandler` mapping. Low priority — no functional impact until CoverImage upload feature is exposed in UI.

---

## 9. Out-of-Scope (confirmed not touched)

- `PublishStep.tsx` — still a placeholder
- `stepOrder.ts` — `isEnabled: false` unchanged for Step 7
- No unpublish endpoint
- No Frequency/RequiresRefresher changes
- No refactoring of old session-based publish endpoint
