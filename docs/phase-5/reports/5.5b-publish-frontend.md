# Phase 5.5b — Frontend Publish Step: Implementation Report

**Date:** 2026-06-14
**Branch:** transval
**Chunk:** 5.5b — New learning wizard Step 7 Publish

---

## 1. Test Results

No automated test runner is installed (no jest/vitest/testing-library in package.json).

- TypeScript baseline (before changes): **0 errors**
- TypeScript with all changes: **0 errors**

Automated unit tests for `isStepReachable` step 7 deferred — tracked as **BACKLOG item 17** (frontend test framework not installed).

---

## 2. Recon Findings

Full recon persisted at `docs/phase-5/reports/5.5b-publish-frontend-recon.md`.

Key findings that shaped implementation:

**Finding 1 — `publish/` route was gitignored.**
`.gitignore` line 4 has bare `publish/` (meant for .NET `dotnet publish` output). This matched and silently suppressed the Next.js route directory `web/src/app/(authenticated)/admin/toolbox-talks/learnings/[talkId]/publish/`. Verified with `git check-ignore -v`. Fixed with negation rule `!web/src/app/**/publish/` added immediately after line 4.

**Finding 2 — `ValidationRunSummary` field names.**
`useValidationRuns` returns objects with `status`, `overallScore`, `overallOutcome`, and `languageCode`. API response is newest-first. Used first-match iteration to build `latestRunByCode: Record<string, ValidationRunSummary>`.

**Finding 3 — `useWorkflowSubscription` returns `TranslationWorkflowStateDto[]`.**
Each entry has `languageCode` and `state`. `AwaitingThirdParty` is the state string for external review.

**Finding 4 — Backend returns direct DTO, not `Result<T>` envelope.**
`POST /api/toolbox-talks/{talkId}/publish` returns `PublishTalkResult { talkId, status, publishedAt }` unwrapped. Frontend reads `response.data`, not `response.data.data`.

**Finding 5 — `validationRuns` is optional third arg to `isStepReachable`.**
Existing callers pass two arguments. Step 7 gate needs runs for the "has a completed run" check. Added optional `validationRuns?: ValidationRunSummary[] | null` — existing callers unaffected (undefined defaults to false for the completed-run check, which is correct: unknown state → not reachable for talks with target languages).

---

## 3. Files Changed in Scope

### `.gitignore` (bug fix — prerequisite)
- Line 5–6: Added `!web/src/app/**/publish/` negation rule
- Without this: `publish/page.tsx` was never tracked by git

### `web/src/features/toolbox-talks/components/learning-wizard/lib/stepOrder.ts`
- Added import: `ValidationRunSummary` from `@/types/content-creation`
- Added `parseTargetCodes` helper (reuses same JSON.parse pattern as the component)
- Extended `isStepReachable` signature: optional `validationRuns?: ValidationRunSummary[] | null` third param
- Case 7 logic:
  - `sections.length === 0` → false
  - `status === 'Published'` → false
  - no target languages → true (English-only: sections exist = ready)
  - target languages set, no/unknown runs → false
  - target languages set, at least one `status === 'Completed'` run → true

### `web/src/features/toolbox-talks/components/learning-wizard/hooks/useStepNavigation.ts`
- Added import: `ValidationRunSummary`
- Added `validationRuns?: ValidationRunSummary[] | null` to `UseStepNavigationOptions`
- Destructured `validationRuns` and passed to both `isStepReachable` call sites (goToStep guard + reachableSteps map)

### `web/src/lib/api/toolbox-talks/toolbox-talks.ts`
- Added `PublishTalkResult` interface after `deleteToolboxTalk`
- Added `publishTalk(talkId: string): Promise<PublishTalkResult>` function — `POST /toolbox-talks/{talkId}/publish`, reads `response.data` (direct DTO, not envelope)

### `web/src/types/toolbox-talks.ts`
- Added `publishedAt: string | null` field to `ToolboxTalk` interface (after `updatedAt`)

### `web/src/features/toolbox-talks/components/learning-wizard/hooks/usePublishTalk.ts` (new)
- `usePublishTalk(talkId)` — mutation wrapper for `publishTalk`
- On success: invalidates `['learnings', talkId]`, `['learnings', 'drafts']`, `['toolbox-talks']`

### `web/src/features/toolbox-talks/components/learning-wizard/steps/PublishStep.tsx` (full replacement)
- Was: 12-line placeholder returning `<div>Step 7 Publish TODO</div>`
- Now: ~490-line implementation with five sub-components:
  - `ContentSummaryPanel` — cover image/placeholder, title, description, documentRef, category badge, refresher/certificate/auto-assign/watch-% badges
  - `ThreeColumnSummary` — Content column (source file, sections, input mode, source language, slideshow/subtitles status), Back-translation scores column (per target language: score % + outcome badge), Quiz column (question count, passing score, shuffle, retry)
  - `AuditMetadataPanel` — reviewerName, reviewerOrg, reviewerRole, documentRef, clientName, auditPurpose; hidden entirely when all fields are null/empty
  - `ExternalReviewWarningBanner` — amber Alert, pluralised ("1 language is…" / "N languages are…"), exact agreed copy, only renders when `awaitingLangs.length > 0`
  - `PublishErrorAlert` — inline destructive Alert parsing axios error (errors[0] → message → err.message)
- Props: `{ talkId: string; isPublishing: boolean; publishError: Error | null }`
- Calls: `useTalk`, `useValidationRuns`, `useWorkflowSubscription` at root
- Exports `PublishSuccessState` (unused — page wrapper navigates immediately on success)

### `web/src/app/(authenticated)/admin/toolbox-talks/learnings/[talkId]/publish/page.tsx` (full replacement)
- Was: 46-line placeholder with no mutation, no props to PublishStep, no navigation
- Now: 97-line page wrapper
  - Calls `useTalk`, `useValidationRuns`, `useStepNavigation` (with `validationRuns`), `usePublishTalk`
  - `handlePublish` — calls `publishTalk(undefined, { onSuccess: toast + router.push('/admin/toolbox-talks/talks/${result.talkId}'), onError: toast })`
  - `publishFooter` — green Button with Rocket icon / Loader2 + "Publishing…" during mutation; disabled while publishing or navigating
  - `WizardLayout` with `canGoNext={false}`, `footer={publishFooter}`, `onStepClick={goToStep}`
  - `PublishStep` receives `talkId`, `isPublishing`, `publishError`

---

## 4. Files Changed Outside Stated Scope

None. All changes are within the 5.5b frontend scope.

---

## 5. Build Output

TypeScript check: **pass, 0 errors**

```
npx tsc --noEmit
(exit code 0)
```

---

## 6. Manual Smoke Evidence

Manual smoke test requires a deployed environment. The following scenarios were specified:

1. **English-only published talk → Step 7 unreachable** — `/publish` redirects immediately to /drafts (step 7 returns false when `status === 'Published'`)
2. **English-only draft with sections → Step 7 reachable, Publish works** — button enabled, on click navigates to `/admin/toolbox-talks/talks/${talkId}`
3. **Multi-language talk with completed runs → Publish works** — per-language rows show score + outcome badge
4. **Multi-language talk with AwaitingThirdParty language → amber banner shows** — "1 language is awaiting external review…" copy present, Publish button enabled

Environment: Development (Railway transval branch). Results to be verified after deploy.

---

## 7. Prompt Requirements Coverage

| Requirement | Status |
|---|---|
| Recon pass persisted to `5.5b-publish-frontend-recon.md` | ✅ Done |
| `isStepReachable` case 7 with correct gate logic | ✅ Done |
| Case 7: zero sections → false | ✅ Done |
| Case 7: sections + no target languages → true | ✅ Done |
| Case 7: sections + target languages + no completed runs → false | ✅ Done |
| Case 7: sections + target languages + one completed run → true | ✅ Done |
| Case 7: already published → false | ✅ Done |
| Unit tests for step 7 cases | ⏸ Deferred — no test runner installed (BACKLOG #17) |
| `usePublishTalk` mutation hook | ✅ Done |
| Publish button in `WizardLayout` footer slot | ✅ Done |
| Content summary panel | ✅ Done |
| Per-language validation cards | ✅ Done |
| Audit metadata panel (hidden when all empty) | ✅ Done |
| Quiz summary column | ✅ Done |
| External review warning banner | ✅ Done |
| Banner copy exactly as specified (sing/plural) | ✅ Done |
| Error display (inline + toast) | ✅ Done |
| Loading state | ✅ Done |
| Post-publish navigation to talks detail | ✅ Done |
| Success toast | ✅ Done |
| Page wrapper wired with mutation + footer + navigation | ✅ Done |
| TypeScript check baseline 0 errors | ✅ Done |
| TypeScript check with changes 0 errors | ✅ Done |
| `.gitignore` `publish/` block fixed | ✅ Done (discovered during implementation) |
| Backend changes | ✅ None (in-scope prohibition respected) |
| Unpublish UI | ✅ None (out of scope) |
| External review cancel UI | ✅ None (out of scope) |

---

## 8. BACKLOG Entries Logged

- **BACKLOG #17** — Frontend test framework not installed; `isStepReachable` step 7 unit tests deferred
- **BACKLOG #18** — Post-publish translation editing gap for AwaitingThirdParty languages (cancel external review backend + UI both missing)
- `.gitignore` `publish/` negation rule surfaced and fixed inline (no BACKLOG entry needed — already closed by fix in `.gitignore`)

---

## Post-deploy smoke evidence (2026-06-14)

**Environment:** Railway transval (production deploy)
**Browser:** Chrome (smoke session)

### Scenario 1 — English-only publishes

**Cross-reference:** Verified via Scenario A of `wizard-skip-regression-fix.md` Post-deploy smoke evidence. Draft talk with no target languages reached Step 7, Publish succeeded, talk transitioned to Published status. Not re-run here.

**Verdict:** ✅ Pass (cross-referenced)

### Scenario 2 — Multi-language with completed validation publishes

**Cross-reference:** Verified via Scenario C of `wizard-skip-regression-fix.md` Post-deploy smoke evidence. Multi-language talk with completed validation runs reached Step 7, Publish button was enabled and clickable, Publish succeeded.

**Verdict:** ✅ Pass (cross-referenced)

### Scenario 3 — AwaitingThirdParty warning banner

**Status:** Smoke deferred — blocked on missing reviewer-action UI.

**Reason for deferral:** Scenario 3 requires driving a talk into AwaitingThirdParty state and then navigating to Step 7 to verify the warning banner renders. During smoke setup, it was discovered that the new wizard exposes no UI to initiate external review on a validated translation — the backend endpoint exists (`POST /api/toolbox-talks/{id}/translations/{lang}/initiate-external-review`) but no UI calls it. Driving the state via direct API call would verify the banner in isolation but not under realistic conditions (where the user reaches Step 7 having traversed the actual workflow). The honest verdict is that the runtime smoke cannot be performed via the user-facing path until that UI ships.

**Code-verified evidence:** The banner implementation at `PublishStep.tsx:434-446` matches the agreed copy from the 5.5b prompt:
- Line 434: `'1 language is awaiting external review'` (singular)
- Line 435: `${count} languages are awaiting external review` (plural)
- Lines 441-443: `When the reviewer submits, their translation will be applied to the published talk automatically. To update [langList] yourself before then, cancel the external review first.`

The `langList` interpolation indicates the banner names which languages are affected — better UX than the prompt specified.

**Verdict:** ⏸ Deferred — code-verified; runtime smoke blocked on consolidated reviewer-action UI BACKLOG entry §23 (see BACKLOG.md).

### Scenario 4 — Direct navigation to /publish with zero sections

**What this verifies:** Graceful handling when a user manually navigates to a Step 7 URL before the step is reachable.

**Steps and observations:**
1. Created a fresh draft talk; did not parse any content (zero sections).
2. Manually typed the `/publish` URL into the browser.
3. Wizard redirected to `/drafts` (the listing page).

**Key observation:** Graceful redirect, no crash, no error state. Step 7 reachability rule correctly rejected the navigation and the system landed the user on a sensible page.

**Verdict:** ✅ Pass

### Summary

| Scenario | Verifies | Verdict |
|---|---|---|
| 1 | English-only publishes | ✅ (cross-ref) |
| 2 | Multi-language with completed validation publishes | ✅ (cross-ref) |
| 3 | AwaitingThirdParty warning banner | ⏸ Deferred (code-verified) |
| 4 | Direct nav to /publish with zero sections | ✅ |

**Overall:** 3 of 4 scenarios verified as passing; 1 deferred with code-verification evidence pending reviewer-action UI work. 5.5b's stated implementation requirements are met.

**BACKLOG entries surfaced during smoke:** Consolidated reviewer-action UI entry §23 (see BACKLOG.md).
